% layout 'default';
<div class="slide ui-corner-all">
<%== $slide->text; %>
</div>
<div class="toolbar">
<%== button prev => url_for(id => $slide->prev) %>
<%== button next => url_for(id => $slide->next) %>
<%== button edit => url_for(action => 'edit') %>
</div>
<div class="notes ui-corner-all"><%= $slide->notes %></div>
